# AWS Lambda requirements for BEA Pumpkin Pi Alexa Skill

# Alexa Skills Kit SDK
ask-sdk-core==1.18.0
ask-sdk-model==1.35.0
ask-sdk-runtime==1.18.0

# Core scientific computing
numpy==1.24.3
scipy==1.10.1

# Optional dependencies for enhanced functionality
# (These may need to be included as Lambda layers due to size constraints)

# Audio processing (if needed for advanced features)
# librosa==0.10.1  # Include only if advanced audio analysis needed
# soundfile==0.12.1  # Include only if file I/O needed

# Additional utilities
boto3==1.26.137  # AWS SDK (usually provided by Lambda environment)
requests==2.31.0  # HTTP client for potential external API calls

# Development and testing (not needed in production)
# pytest==7.4.0
# pytest-mock==3.11.1